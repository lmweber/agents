# Project instructions for Claude Code

This project/workspace contains the following folders:

- `folder_01/`: Details
- `folder_02/`: Details
- `folder_03/`: Details

Git repositories are stored in the subdirectories where needed. The overall project/workspace directory does not contain a Git repository.

## Scope

- Treat this directory as the project/workspace root.
- Do not search outside this directory.
- Do not inspect parent directories such as `~`, or other directories such as `~/Documents`, `~/Desktop`, or `~/Downloads`.
- You may read files within the project/workspace for context.

## Editing

- Do not modify files unless explicitly asked to or given permission.

## Git

- Do not create commits, amend commits, reset, rebase, merge, tag, or push.
- Leave all changed files unstaged unless explicitly asked.

## At the end of a task

- List any suggested files to change.
- For coding-related tasks, also suggest a commit message, but do not commit.
- If you have been given permission to change files, list any files that have been changed.
